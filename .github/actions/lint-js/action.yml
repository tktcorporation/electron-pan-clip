name: 'JavaScriptコードのLint'
description: 'ESLint、TypeScript、Prettierによるコード品質チェック'

inputs:
  node-version:
    description: 'Node.jsのバージョン (空の場合は.node-versionを使用)'
    required: false
    default: ''
  lint-command:
    description: 'ESLintを実行するコマンド'
    required: false
    default: 'npm run lint'
  format-command:
    description: 'フォーマットをチェックするコマンド'
    required: false
    default: 'npm run format:check'
  typecheck-command:
    description: '型チェックを実行するコマンド'
    required: false
    default: 'npm run typecheck'
  working-directory:
    description: '作業ディレクトリ'
    required: false
    default: '.'
  check-uncommitted:
    description: '未コミットの変更をチェックするかどうか'
    required: false
    default: 'true'

runs:
  using: "composite"
  steps:
    - name: Node.js設定
      uses: ./.github/actions/node-setup
      with:
        node-version: ${{ inputs.node-version }}
        registry-url: 'https://registry.npmjs.org'
        package-path: ${{ inputs.working-directory }}
    
    - name: add antfu/ni
      shell: bash
      run: |
        npm i -g @antfu/ni
    
    - name: Install dependencies
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: nci
    
    - name: ESLint
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: ${{ inputs.lint-command }}
    
    - name: Check Format
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: ${{ inputs.format-command }}
    
    - name: Type Check
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: ${{ inputs.typecheck-command }}
    
    - name: Check for uncommitted changes
      if: inputs.check-uncommitted == 'true'
      shell: bash
      run: |
        if [[ -n $(git status --porcelain) ]]; then
          echo "未コミットの変更があります。まず以下の変更をコミットしてください："
          git status --porcelain
          exit 1
        fi 